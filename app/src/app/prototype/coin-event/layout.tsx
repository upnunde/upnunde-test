import type { ReactNode } from "react";

import "@/components/prototype/tossface.css";

/** Tossface — 코인 이벤트 프로토타입 페이지에서만 로드 */
export default function PrototypeCoinEventLayout({ children }: { children: ReactNode }) {
  return children;
}
