{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -d app ]; then npm --prefix app ci; else npm ci; fi",
  "buildCommand": "if [ -d app ]; then npm --prefix app run build && rm -rf .next && cp -R app/.next .next; else npm run build; fi",
  "outputDirectory": ".next"
}
