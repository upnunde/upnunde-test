/**
 * 페이지·카드 본문 인셋 — design-system spacing-20(20px) = Tailwind `p-5` / `px-5`
 */

/** 카드 섹션 헤더·본문·푸터 좌우 인셋 */
export const PAGE_CONTENT_PAD_X_CLASS = "px-5";

/** 카드 본문(폼 필드 묶음) */
export const PAGE_CONTENT_BODY_CLASS = "px-5 py-5";

/** 카드 하단 액션 바 */
export const PAGE_CONTENT_FOOTER_CLASS = "px-5 py-4";

/** 메인 스크롤 영역 — max-width 컨테이너 바깥 가로 여백 */
export const PAGE_SCROLL_GUTTER_CLASS = "px-5";

/** 서브 페이지 상단 바(뒤로가기·제목) */
export const PAGE_SUBHEADER_CLASS =
  "flex h-16 shrink-0 items-center justify-center border-b border-border-10 bg-white px-5 py-0";

/** 스크롤 + 중앙 정렬 레이아웃(리소스 상세·목록 등) */
export const PAGE_SCROLL_COLUMN_CLASS =
  "flex-1 overflow-y-auto flex flex-col items-center py-8 gap-4 px-5";
